/*
This file was generated by the Surface compiler.
*/

// A <details> element's `open` state is client-side DOM state — every LiveView patch (e.g. the
// re-render after a settings save) resets it, collapsing the advanced editor mid-edit. Remember
// the state across patches.
let PersistDetails = {
	mounted() {
		this.el.addEventListener("toggle", () => {
			this.wasOpen = this.el.open;
		});
	},
	updated() {
		if (this.wasOpen) this.el.open = true;
	},
};

export { PersistDetails };
