<Bonfire.UI.Social.WidgetUsersLive 
  :if={current_user(assigns) || Settings.get([Bonfire.UI.Social.WidgetAdminsLive, :show_guests], true, assigns)}
  widget_title={e(assigns, :widget_title, "Admins")}
  current_user={current_user(assigns)}
/>
