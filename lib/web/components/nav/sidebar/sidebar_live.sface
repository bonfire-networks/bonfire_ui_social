
<nav
  role="navigation"
  class="flex-col justify-end hidden md:flex w-72"
>
  <div class="top-[80px] bottom-[16px] w-72">
    <div class="w-full h-full">
      <div class="flex flex-col space-y-4">
        <div class="">
          <Bonfire.UI.Social.SmartInputLive
            :if={current_user(assigns)}
            current_user={current_user(assigns)}
            reply_to_id={@reply_to_id}
            thread_id={@thread_id}
            create_activity_type={e(assigns, :create_activity_type, nil)}
            to_circles={e(assigns, :to_circles, [])}
            smart_input_prompt={@smart_input_prompt}
            smart_input_text={@smart_input_text}
          />
        </div>

        <Bonfire.UI.Social.WidgetLive 
          widget_title="Notifications"
          current_user={current_user(assigns)}
        >
          <Bonfire.UI.Social.WidgetNotificationLive 
            current_user={current_user(assigns)}
            id="widget_notification"
          />
        </Bonfire.UI.Social.WidgetLive>

         <Bonfire.UI.Social.WidgetLive 
          widget_title="Direct Messages"
          current_user={current_user(assigns)}
        >
          <Bonfire.UI.Social.WidgetMessagesLive 
            current_user={current_user(assigns)}
            id="widget_direct_messages"
          />
        </Bonfire.UI.Social.WidgetLive>
            
        <!-- <Bonfire.UI.Social.WidgetLive 
          widget_title="Admins"
          current_user={current_user(assigns)}
        >
          <Bonfire.UI.Social.WidgetAdminsLive 
            current_user={current_user(assigns)}
            id="widget_admins"
          />
        </Bonfire.UI.Social.WidgetLive> -->
        <!-- <div
          :for={{component, component_assigns} <- Bonfire.Common.Config.get([:ui, :sidebar_components], [])}
        >
          <Surface.Components.Dynamic.Component
            :if={module_enabled?(component)}
            module={component}
            page={@page}
          />
        </div> -->

      </div>
    </div>
  </div>
</nav>




