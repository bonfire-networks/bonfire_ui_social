<Bonfire.UI.Social.FeedLive 
    id={e(@feed_id, :fediverse)}
    feed_id={e(@feed_id, nil)}
    page_title={@page_title}
    current_user={@current_user}
    feed={@feed}
    page_info={@page_info}
    loading={e(@loading, false)}
/>
